# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: build
"on": pull_request
jobs:
  build_demo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: "npm"
      - name: Install dependencies
        run: npm ci
      - name: Build openbridge-webcomponents
        run: cd packages/openbridge-webcomponents && npm run build
      - name: Build openbridge-webcomponents-vue
        run: cd packages/openbridge-webcomponents-vue && npm run build
      - name: Build vue-demo
        run: cd packages/vue-demo && npm run build
      - uses: actions/upload-artifact@v4
        with:
          name: vue-demo
          path: packages/vue-demo/dist/
  build_storybook:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: "npm"
      - name: Install dependencies
        run: npm ci
      - name: Build storybook
        run: cd packages/openbridge-webcomponents && npm run build-storybook
      - uses: actions/upload-artifact@v4
        with:
          name: storybook
          path: packages/openbridge-webcomponents/storybook-static/
