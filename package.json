{
  "name": "root",
  "private": true,
  "scripts": {
    "build:lib": "yarn workspaces foreach -Rpt --from '{@tibnor/openbridge-webcomponents,@tibnor/openbridge-webcomponents-vue}' run build",
    "format:all": "yarn workspaces foreach -Ap run format"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@4.0.2"
}
