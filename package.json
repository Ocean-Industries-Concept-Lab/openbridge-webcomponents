{
  "name": "root",
  "private": true,
  "scripts": {
    "build:lib": "yarn workspaces foreach -Rpt --from '{openbridge-webcomponents,openbridge-webcomponents-vue}' run build",
    "format:all": "yarn workspaces foreach -Ap run format"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^7.4.2"
  },
  "packageManager": "yarn@4.0.2"
}
