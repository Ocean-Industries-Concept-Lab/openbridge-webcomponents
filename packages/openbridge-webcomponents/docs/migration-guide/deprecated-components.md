| Element                                                                                                                                                               | Use instead                                                                                                                                                    |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `obc-instrument-field`                                                                                                                                                | `obc-readout`; see [Instrument Field (deprecated)](?path=/docs/instruments-instrument-field-deprecated--docs)                                                  |
| `obc-azimuth-thruster-labeled`                                                                                                                                        | `obc-azimuth-thruster` with an `obc-readout` beside it                                                                                                         |
| `obc-notification-badge-button`                                                                                                                                       | `obc-icon-button`: the icon goes in its default slot and the text in the `label` slot with `hasLabel`; `openLeft`, `openRight` and `indent` have no equivalent |
| `obc-corner-line`, `obc-direction-line`, `obc-end-point-line`, `obc-horizontal-line`, `obc-line-cross`, `obc-line-overlap`, `obc-three-way-line`, `obc-vertical-line` | the `@oicl/connector-diagram` package                                                                                                                          |
