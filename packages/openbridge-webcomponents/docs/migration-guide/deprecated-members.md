| Element                                                                                                                         | Deprecated                                                                                    | Use instead                                                                            |
| ------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| `obc-alert-icon`                                                                                                                | `type`                                                                                        | `alertType`                                                                            |
| `obc-compass`, `obc-compass-flat`, `obc-compass-sector`, `obc-rate-of-turn`, `obc-rot-indicator`, `obc-watch`, `obc-watch-flat` | `rotationsPerMinute`                                                                          | `rateOfTurnDegreesPerMinute`; the old property applies only while the new one is unset |
| `obc-depth-actual`                                                                                                              | `instrumentRange`                                                                             | `maxDepth`                                                                             |
| `obc-fan`, `obc-motor`, `obc-pump`                                                                                              | `speedInPercent`                                                                              | `speed` with `speedUnit`; `speed` wins when both are set                               |
| `obc-tab-item`, and the `tabs` of `obc-tab-row`                                                                                 | `hasBadge`, `badgeCount`, `badgeType`, `badgeSize`, `badgeShowNumber`, `showLeadingBadgeIcon` | `badges`                                                                               |
| `obc-wind-indicator`                                                                                                            | `angle`                                                                                       | `currentWindFromDirection`                                                             |
